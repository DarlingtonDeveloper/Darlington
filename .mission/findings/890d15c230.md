# Summary

Applied 3 verify fixes to MC Dashboard:

1. **Inline style → Tailwind** (`mission-view.tsx`): Converted `style={{ gridTemplateColumns: "3fr 2fr", height: "calc(100vh - 240px)" }}` to Tailwind classes `grid-cols-[3fr_2fr] h-[calc(100vh-240px)]`.

2. **Wire audit/checkpoints** (`mc-client.tsx`): Replaced hardcoded `audit={[]}` and `checkpoints={[]}` with `audit={mcState.audit ?? []}` and `checkpoints={mcState.checkpoints ?? []}`.

3. **"use client" directives**: Added to `mission-view.tsx`, `dashboard-header.tsx`, and `worker-card.tsx`.

`tsc --noEmit` passes cleanly.

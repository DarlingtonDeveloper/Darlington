# Summary

Created `components/mc/token-usage-panel.tsx` — a "use client" component that displays token usage data.

## Implementation

- **Props:** `{ tokens: TokenSummary }` imported from `lib/mc/types`
- **Budget progress bar:** Shown when `budget_limit > 0`, with color thresholds (accent < 70%, yellow 70-90%, red > 90%). Displays used/limit and remaining.
- **Per-worker grid:** 4-column grid (Worker, In, Out, Cost) showing each session's token counts and cost
- **Total row:** Bold summary row with summed input/output tokens and total cost
- **Zero state:** "No token data yet" muted text when no sessions exist
- **Formatting:** Large numbers with `toLocaleString("en-US")` for commas; costs as USD with 2 decimal places
- **Styling:** Dark theme, Tailwind CSS only, follows existing component patterns (mono font, muted color palette)
- **Export:** `export default function TokenUsagePanel`
